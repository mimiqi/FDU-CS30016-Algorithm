请独立分析下面算法的渐近时间复杂度，给出关于 n 的紧确 Θ 界和推导过程。n 为正整数；单位代价 RAM 模型，整数运算、比较、赋值、floor(log2(n)) 与 work() 均为 O(1)，整数不溢出。递归不使用记忆化，每次调用都实际执行。请检查取整与基例的影响；若不能确定紧确界，请明确说明。请勿读取工作区、调用其他模型或修改文件。

procedure F(n):
    if n <= 3:
        work()
        return
    k = floor(log2(n))
    r = floor(n / k)
    F(r)
    F(n - r)
    for i = 1 to n:
        work()

请直接给出最终完整分析。
