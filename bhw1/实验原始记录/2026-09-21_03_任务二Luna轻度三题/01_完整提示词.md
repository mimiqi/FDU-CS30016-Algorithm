请独立分析以下三个算法的渐近时间复杂度，分别给出关于正整数 n 的紧确 Θ 界和推导过程。采用单位代价 RAM 模型，整数算术、比较、赋值、floor(log2(n)) 和 work() 均为 O(1)，整数不溢出。递归没有记忆化，各次调用独立执行。请检查取整和停止条件；不能确定紧确界时请明确说明。请勿读取工作区、调用其他模型或修改文件。

算法 A：
procedure A(n):
    for i = 2 to n:
        j = i
        while j <= n:
            work()
            if j > floor(n / j):
                break
            j = j * j

算法 B：
procedure B(n):
    if n <= 2:
        work()
        return
    B(floor(n / 2))
    B(ceil(n / 2))
    for i = 1 to floor(n / floor(log2(n))):
        work()

算法 C：
procedure C(n):
    if n <= 3:
        work()
        return
    k = floor(log2(n))
    r = floor(n / k)
    C(r)
    C(n - r)
    for i = 1 to n:
        work()

请直接给出 A、B、C 的完整分析。
